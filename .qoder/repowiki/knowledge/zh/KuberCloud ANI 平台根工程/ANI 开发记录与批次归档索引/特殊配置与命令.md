批次完成需严格按 README 中“批次完工更新流程”顺序执行：`make test` → 新建 `{批次名}.md`（基于 `TEMPLATE.md`）→ 追加 `README.md` 表格 → 更新 `repo/CURRENT-SPRINT.md` 与 `ANI-06-开发计划.md` Section 零 → `git commit`；live gate 通过 `--evidence-output` / `ANI_*_LIVE_EVIDENCE_OUTPUT` 环境变量将脱敏 JSON 写入 `live-evidence/`。