Protobuf 代码生成由 `repo/api/proto/buf.gen.yaml` 驱动，需安装 `protoc-gen-go`、`protoc-gen-go-grpc`、`protoc-gen-grpc-gateway` 插件后执行 `buf generate`（或 make gen-core-sdk）将输出写入 `pkg/generated/pb`；兼容性校验基于 `core-v1-compatibility-baseline.yaml` 中的 allowed/forbidden 规则对 `api/openapi/v1.yaml` 进行 diff 检查。