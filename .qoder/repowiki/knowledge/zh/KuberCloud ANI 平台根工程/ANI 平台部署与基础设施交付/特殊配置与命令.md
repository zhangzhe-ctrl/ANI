本地开发依赖：`make deps` 或 `docker compose -f deploy/docker/docker-compose.yml up -d` 启动全部基础设施，`make deps-down` 停止；GPU 相关额外启用 `--profile tools` 启动 Attu、`--profile auth` 启动 Dex；基础设施基线校验命令见各 manifests 子目录 README（如 `make validate-infra`）。