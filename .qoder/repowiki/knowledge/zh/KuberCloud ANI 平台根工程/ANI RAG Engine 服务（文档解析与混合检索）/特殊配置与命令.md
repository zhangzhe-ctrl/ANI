通过 `python -m main` 或 `uvicorn main:app --host 0.0.0.0 --port 8001` 启动；需准备 `.env` 文件（或环境变量）提供 `MILVUS_ADDR`、`DATABASE_URL`/`PG_DSN`、`NATS_URL`、`embedding_api_base`、`vllm_*`、`minio_*` 等；生产环境依赖外部 Milvus、PostgreSQL、NATS、MinIO 及 AI 推理服务。