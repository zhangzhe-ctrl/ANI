SDK 源码为生成产物，不可直接编辑；新增/修改 API 需更新对应层的 `sdk-metadata.json` 后重新运行 `scripts/gen_sdk_alpha.py`。各语言示例位于 `examples/basic/main.go`、`examples/Basic.java`、`examples/basic.py`、`examples/basic.mjs`，冒烟测试位于 `smoke.py` / `Smoke.java` / `client_test.go` / `smoke.mjs`。