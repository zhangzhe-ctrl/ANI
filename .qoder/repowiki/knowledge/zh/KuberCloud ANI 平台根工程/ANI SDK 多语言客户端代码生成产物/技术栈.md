由 `scripts/gen_sdk_alpha.py` 根据 OpenAPI 风格的 `sdk-metadata.json` 生成；Go 使用 `net/http` + `encoding/json`，Python 使用标准库 `urllib` + `json`，TypeScript 以 ES Module (`*.mjs`) 形式发布，Java 采用 `com.kubercloud.ani.core/services` 包结构。