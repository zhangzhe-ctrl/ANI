`npm run gen-api` 会调用 `openapi-typescript` 从 `../../api/openapi/services/v1.yaml` 生成 `src/api/schema.d.ts`，并执行 `scripts/gen-core-schema.mjs` 生成 Core API 类型；开发时 `vite dev` 将 `/api` 代理到 `http://localhost:8080`（可切换至 4010）。