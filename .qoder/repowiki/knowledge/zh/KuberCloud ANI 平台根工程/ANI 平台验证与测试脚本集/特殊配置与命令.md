Live Gate 与 KB E2E 脚本需先启动 ANI 平台（gateway、postgres、向量库等）并在本地或集群中暴露端口；多数脚本通过命令行参数传入 `--gateway-url`、`--token`、`--tenant-id`、`--kubeconfig` 等凭据。部分脚本硬编码镜像名与 digest（如 `run_instance_sandbox_stateless_live_gate.py` 中的 `IMAGE`/`IMAGE_DIGEST`）。