服务启动需设置环境变量 `DATABASE_URL`、`NATS_URL`、`REDIS_URL`、`GRPC_PORT`（默认 9104）、`HEALTH_PORT`（默认 9204）以及可选的 `OUTBOX_ENABLED`、`OUTBOX_POLL_INTERVAL_MS`、`OUTBOX_BATCH_SIZE`；构建命令即标准 `go build ./...`，模块依赖通过 `replace github.com/kubercloud/ani/pkg => ../../pkg` 指向本地共享包。