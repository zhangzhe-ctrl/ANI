CloudWeGo Hertz HTTP 框架；gRPC 调用远端 auth-service（`kubercloud/ani/pkg/generated/pb/auth/v1`）；JWT 与 sandbox token 校验使用 `kubercloud/ani/pkg/security/sandboxtoken`；缓存后端通过 `kubercloud/ani/pkg/ports.CacheStore` 抽象；UUID 使用 `google/uuid`。