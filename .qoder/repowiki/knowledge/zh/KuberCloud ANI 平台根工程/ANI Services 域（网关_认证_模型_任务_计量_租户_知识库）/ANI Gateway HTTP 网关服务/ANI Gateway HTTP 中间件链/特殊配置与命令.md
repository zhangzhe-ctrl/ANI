启动时需调用 `StartAuditWorker()` 启动审计后台 goroutine；通过环境变量控制行为：`ANI_AUTH_MODE=dev` 启用开发模式（允许 `X-Dev-Tenant-ID`/`X-Dev-User-ID` 绕过鉴权），`GATEWAY_RATE_LIMIT_REQUESTS` 与 `GATEWAY_RATE_LIMIT_WINDOW` 配置限流阈值与时窗，`AUTH_SERVICE_ADDR` 指定 auth-service gRPC 地址。