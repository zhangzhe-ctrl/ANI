监听地址默认 `:8080`，可通过 `GATEWAY_LISTEN_ADDR` 覆盖；Redis 连接优先读 `GATEWAY_REDIS_URL`，其次 `REDIS_URL`，再回退到 `redis://:ani_dev_password@127.0.0.1:6379/0`；开发鉴权模式通过设置 `ANI_AUTH_MODE=dev` 并使用 `X-Dev-Tenant-ID`/`X-Dev-User-ID` 头绕过认证服务。