构建需仓库根目录存在 `go.work`/`go.work.sum`（Dockerfile 中 COPY 了它们），并通过 `replace github.com/kubercloud/ani/pkg => ../../pkg` 指向本地 pkg 模块；运行前需设置 `DATABASE_URL`、`REDIS_URL`、`NATS_URL`、`GRPC_PORT`、`HEALTH_PORT` 以及 `AUTH_JWT_*`、`AUTH_OIDC_*` 系列环境变量。