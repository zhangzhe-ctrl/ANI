Go 1.25；gRPC + protobuf 作为 RPC 协议；PostgreSQL（pgx/v5）持久化 API Key、Session、Refresh Token；Redis 用作缓存与会话存储；NATS 由 bootstrap 注入；JWT 采用自实现的 RS256（RSA-PKCS1v15 + SHA-256）签发与校验，无第三方 JWT 库；OIDC 支持 Dex 风格默认 URL 推导与 JWKS 动态密钥拉取。