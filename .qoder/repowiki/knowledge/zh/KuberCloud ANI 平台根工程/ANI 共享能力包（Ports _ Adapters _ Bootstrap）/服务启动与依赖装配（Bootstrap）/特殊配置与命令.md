进程启动统一调用 `bootstrap.MustConnect(cfg)` 获取 `*Deps`，随后调用 `bootstrap.RunGRPC(port, register, deps)` 或 `RunHealthProbe(deps)`；`MustConnect` 在任一依赖连接失败时直接 `os.Exit(1)`，因此服务必须确保环境变量（DATABASE_URL、NATS_URL、REDIS_* 等）正确设置后再启动。