Go 标准库 + `github.com/jackc/pgx/v5/pgxpool`（PostgreSQL 连接池）、`github.com/nats-io/nats.go`（NATS JetStream，预建 ANI_TASKS/ANI_EVENTS 流）、`github.com/redis/go-redis/v9` UniversalClient（standalone/sentinel/cluster）、`google.golang.org/grpc`（gRPC 服务器，含 logging/recovery 拦截器与 reflection）。