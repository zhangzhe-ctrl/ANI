`docs/ops/scripts/new-feature.sh <scope> <slug>` 用于在干净的 main 分支上创建 `feature/<scope>/<slug>` 分支并设置 `.gitmessage` 提交模板；AI 执行计划中多次要求进入 `repo/` 目录运行 `make validate-services-contract`、`python scripts/validate_inference_service_contract.py` 等命令来校验契约变更。