- AI 执行计划与设计方案统一使用日期前缀文件名（如 `2026-08-14-inference-service-stage-b-contract.md`），便于按时间排序与归档。
- plans 文档以带 checkbox 的任务清单组织步骤，并在每个任务下明确列出 Consumes/Produces 接口、目标文件与预期验证命令。
- 特性文档包采用 PRD/SPEC/UX 三件套分离结构，并通过 `references/` 子目录集中放置同级或上游模块的引用文档，根 README 以表格形式索引主文档与引用来源。
- UI 相关参考统一归入 `references/UI规范/` 并按 TDesign 2.0 版本命名，避免与业务文档混放。